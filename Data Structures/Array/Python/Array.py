import numpy as np

# //Instantiation
emtpy = np.array(None)
teams = np.array(['Knicks', 'Mets', 'Giants'])

# //Literal notation
otherTeams = ['Nets', 'Patriots', 'Jets']

# //size
print('Size:', len(otherTeams))

# //Access
print(teams[0])

# //sort
SORTED = teams.sort()
print('Sorted:', teams)

# //search
